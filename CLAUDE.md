# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## If this is an ARM Mac (or a GPU workstation)

Paused here since Intel Mac has no `torch` wheel for macOS x86_64/Python
3.13, blocking `auto_knowledge_graph`'s embedding step. If the current
machine is Apple Silicon or has a GPU, tell the user and offer to resume
Phase 5 — see `TODO.md` for the exact resume checklist.

## GitHub repo metadata

Confirm with the user before changing this repo's GitHub topics, description,
or visibility.
